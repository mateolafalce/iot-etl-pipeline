[project]
name = "iot-tests"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pytest>=8.0.0",
    "polars>=0.20.0",
    "numpy>=1.26.0",
]

[tool.pytest.ini_options]
testpaths = ["."]
